<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:core="clr-namespace:BaseLogApp.Core.Models" x:Class="BaseLogApp.Views.JumpsPage" Title="{Binding Title}">
   
    <Grid RowDefinitions="Auto,*" Padding="12">
        <!-- Barra di ricerca con clear automatico -->
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
           
            <SearchBar x:Name="Search" Placeholder="Cerca in numero, data, oggetto, tipo..." Text="{Binding Query, Mode=TwoWay}" TextChanged="OnSearchTextChanged" />
           
        </Grid>
        <!-- Lista collegata al ViewModel -->
        <CollectionView Grid.Row="1"
                ItemsSource="{Binding FilteredItems}"
                ItemSizingStrategy="MeasureAllItems">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="core:JumpListItem">
                    <Grid Padding="8"
            ColumnDefinitions="Auto,*"
            RowDefinitions="Auto,Auto"
            ColumnSpacing="10">
                        <Label Grid.RowSpan="2" Grid.Column="0"
               Text="{Binding NumeroSalto}"
               FontSize="32"
               FontAttributes="Bold"
               VerticalOptions="Center"
               HorizontalTextAlignment="Center"
               TextColor="White" />
                        <Label Grid.Row="0" Grid.Column="1"
               Text="{Binding Data}"
               FontAttributes="Bold"
               TextColor="White" />
                        <HorizontalStackLayout Grid.Row="1" Grid.Column="1" Spacing="6">
                            <Label Text="{Binding Oggetto}" FontSize="12" MaxLines="1"
                 LineBreakMode="TailTruncation" TextColor="Gainsboro" />
                            <Label Text="- " FontSize="12" TextColor="Gainsboro" />
                            <Label Text="{Binding TipoSalto}" FontSize="12" MaxLines="1"
                 LineBreakMode="TailTruncation" TextColor="Gainsboro" />
                        </HorizontalStackLayout>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>


    </Grid>
</ContentPage>